[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace=packages/core && npm run build --workspace=packages/api"

[deploy]
startCommand = "node packages/api/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3