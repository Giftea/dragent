[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install && npm run build --workspace=packages/core && npm run build --workspace=packages/api"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3