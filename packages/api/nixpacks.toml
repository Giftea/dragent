[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = [
  "cd ../.. && npm run build --workspace=packages/core",
  "npm run build"
]

[start]
cmd = "node dist/index.js"